#include <bits/stdc++.h>

using namespace std;

class solution {

public:

int minimumTransfers(int A, int B) {

    for(int x = 0; x < A; x++) {

        int alice = A - x;

        int bob = B + x;

        if(alice % bob == 0) {

            return x;
        }
    }

    return -1;
}

};
